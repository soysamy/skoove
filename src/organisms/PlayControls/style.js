import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    aspectRatio: 150 / 76,
    justifyContent: 'center',
    alignItems: 'center',
  },
  favoriteIcon: {
    //position: 'absolute',
    //right: 10,
  },
  playControlWrapper: {
    flexDirection: 'row',
    justifyContent: 'center',
    marginBottom: 10,
  },
});
